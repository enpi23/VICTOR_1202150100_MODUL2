<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kelompok6.co.wash.victor_1202150100_modul2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MainTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/NoActTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".DineInActivity" />
        <activity android:name=".TakeAwayActivity" />
        <activity android:name=".DaftarMenu"/>
        <activity
            android:name=".DetailMenu"
            android:parentActivityName="kelompok6.co.wash.victor_1202150100_modul2.DaftarMenu">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".DetailMenu" />
        </activity>
    </application>

</manifest>